import { styled } from '../../../stitches.config';

export const Container = styled('header', {
  background: '$gray900',
  height: '7.5rem',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
})

